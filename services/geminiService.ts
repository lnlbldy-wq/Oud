
// AI Service is currently disabled/removed from the UI.
// Keeping this stub to prevent import errors if it's referenced anywhere else, 
// but removing the dependency on process.env and @google/genai.

export const generateOudPoetry = async (): Promise<string> => {
  return "";
};
